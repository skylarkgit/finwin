name: finwin
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - requests
  - feedparser
  - beautifulsoup4
  - pydantic>=2.0
  - pip:
      - trafilatura
      - yfinance
      - pdfminer.six
      - httpx
